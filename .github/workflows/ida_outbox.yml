name: IDA Outbox Runner

on:
  push:
    branches: [ main ]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
  - uses: actions/checkout@v4

  - run: |
      echo "Runner OK"
      ls -la agent_outbox || true

